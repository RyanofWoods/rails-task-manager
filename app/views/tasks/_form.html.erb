<%= simple_form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label :completed, hidden: @task.new_record? %>
  <%= f.check_box :completed, hidden: @task.new_record? %>
  <%= f.submit %>
<% end %>